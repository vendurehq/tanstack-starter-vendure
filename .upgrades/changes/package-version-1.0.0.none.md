# No downstream impact

## Reason

Sets `package.json` `version` to `1.0.0` so it matches the existing initial release manifest at `.upgrades/releases/v1.0.0`. Runtime behavior, dependencies, configuration, and generated-code inputs are unchanged.
