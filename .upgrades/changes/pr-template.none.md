# No downstream impact

## Reason

The pull request template only guides upstream contributors when opening a pull request. It does not alter generated storefront source, runtime behavior, dependencies, configuration, or the downstream upgrade process.
